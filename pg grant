SELECT current_database(), current_user;

SELECT
  inet_server_addr() AS host,
  inet_server_port() AS port,
  current_database() AS db,
  current_user AS usr;

SELECT rolname, quote_ident(rolname) AS ident
FROM pg_roles
WHERE rolname ILIKE '%app_user%';

Copy the ident value from the query result grid and run:

GRANT USAGE, CREATE ON SCHEMA public TO <paste_ident_here>;




installation pgadmin
# 0) See how pgAdmin is installed today (optional but useful)
dpkg -l | grep -i pgadmin || true
snap list | grep -i pgadmin || true
docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Ports}}' | grep -i pgadmin || true

# 1) Add the official pgAdmin APT repo (as per pgadmin.org)
sudo apt update
sudo apt install -y curl gpg ca-certificates

curl -fsS https://www.pgadmin.org/static/packages_pgadmin_org.pub \
  | sudo gpg --dearmor -o /usr/share/keyrings/packages-pgadmin-org.gpg

echo "deb [signed-by=/usr/share/keyrings/packages-pgadmin-org.gpg] https://ftp.postgresql.org/pub/pgadmin/pgadmin4/apt/$(lsb_release -cs) pgadmin4 main" \
  | sudo tee /etc/apt/sources.list.d/pgadmin4.list > /dev/null

sudo apt update

# 2) If you use pgAdmin in the browser: install/upgrade web mode
sudo apt install -y pgadmin4-web

# 3) Configure (only needed once; safe to re-run)
sudo /usr/pgadmin4/bin/setup-web.sh

# 4) Verify installed package versions
dpkg -l | grep -i pgadmin

install pg on linux local
set -euo pipefail

# Install PostgreSQL server
sudo apt update
sudo apt install -y postgresql postgresql-contrib

# Start + enable
sudo systemctl enable --now postgresql

# Verify cluster + port
sudo pg_lsclusters
sudo ss -ltnp | grep 5432

# Create role + DB (your commands)
sudo -u postgres psql -c "CREATE ROLE admin WITH LOGIN PASSWORD 'password';"
sudo -u postgres psql -c "ALTER ROLE admin CREATEDB;"
sudo -u postgres psql -c "CREATE DATABASE admin OWNER admin;"

